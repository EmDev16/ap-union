<x-layout title="Contactberichten"><h1 class="text-2xl font-bold">Contactberichten</h1>@if (session('status'))<p class="mt-4 text-green-700">{{ session('status') }}</p>@endif<div class="mt-6 space-y-4">@forelse ($contacts as $contact)<article class="rounded border p-4"><h2 class="font-semibold">{{ $contact->subject }}</h2><p class="text-sm">{{ $contact->type === 'feedback' ? 'Feedback' : 'Vraag' }} · {{ $contact->name }} · {{ $contact->email }} · {{ $contact->created_at->format('d/m/Y H:i') }}</p><p class="mt-2 whitespace-pre-line">{{ $contact->message }}</p>@if ($contact->is_answered)<p class="mt-3 text-green-700">Beantwoord</p>@else<form method="POST" action="{{ route('admin.contacts.update', $contact) }}" class="mt-3">@csrf @method('PATCH')<button class="rounded bg-indigo-600 px-4 py-2 text-white">Markeer als beantwoord</button></form>@endif</article>@empty <p>Nog geen berichten.</p>@endforelse</div></x-layout>
