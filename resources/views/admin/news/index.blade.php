<x-layout title="Nieuws beheren"><div class="flex items-center justify-between"><h1 class="text-2xl font-bold">Nieuws beheren</h1><a href="{{ route('admin.news.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-white">Nieuw bericht</a></div>@if (session('status')) <p class="mt-4 text-green-700">{{ session('status') }}</p> @endif<div class="mt-6 space-y-3">@forelse ($newsItems as $news)<article class="rounded border p-4"><h2 class="font-semibold">{{ $news->title }}</h2><p class="text-sm">{{ $news->published_at->format('d/m/Y H:i') }} · {{ $news->user->name }}</p><div class="mt-3 flex gap-3"><a class="underline" href="{{ route('admin.news.edit', $news) }}">Bewerk</a><form method="POST" action="{{ route('admin.news.destroy', $news) }}">@csrf @method('DELETE')<button class="text-red-700 underline">Verwijder</button></form></div></article>@empty <p>Nog geen nieuws.</p>@endforelse</div></x-layout>
